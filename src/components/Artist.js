import React, { Component } from 'react';

export default function Artist(props) {
    
        return (
            <div>

            <p>{props.name}: {props.info}</p>

            </div>
        );
    
}